<!DOCTYPE html>
<html>
<head>
    <title>Список клиентов</title>
    <meta charset="utf-8" />
</head>
<body>
<h1>Список клиентов</h1>
<p><a href="/create">Добавить клиента</a></p>
<table>
    <tr><th>ФИО</th><th>Адрес</th><th>Телефон</th><th>э-мейл</th><th>Вип-статус</th></tr>
    {{#each clients}}
        <tr>
            <td>{{this.name_client}}</td>
            <td>{{this.adress}}</td>
            <td>{{this.phone}}</td>
            <td>{{this.e-mail}}</td>
            <td>{{this.vip}}</td>
            <td>
                <a href="/edit/{{this.id_client}}">Изменить</a>|
                <form action="delete/{{this.id_client}}" method="POST" style="display:inline;">
                    <input type="submit" value="Удалить" />
                </form>
            </td>
        </tr>
    {{/each}}
</table>
</body>
<html>
